function adding(){
    var first_number = 2;
    var second_number = 3;
    var sum = parseInt(first_number)+parseInt(second_number);//converting the numbers into integers and adding the two numbers.
    console.log(sum);
 }
 adding();